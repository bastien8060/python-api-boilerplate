[Unit]
Description=Keycloak Server
After=network.target

[Service]
ExecStart=/usr/bin/docker run --rm --name keycloak -p 8080:8080 -e KEYCLOAK_ADMIN=admin -e KEYCLOAK_ADMIN_PASSWORD=admin quay.io/keycloak/keycloak:latest start --optimized
Restart=always

[Install]
WantedBy=multi-user.target
